<div class="navbar">
  <h1><%= link_to "Wikinimous", articles_path %></h1>
  <button type="button"><%= link_to "New Article", articles_new_path %></button>
</div>
<% @articles.each do |article| %>
  <div>
    <h1><%= link_to article.title, article_path(article.id) %></h1>
    <p><%= article.content %></p>
  </div>
<% end %>
